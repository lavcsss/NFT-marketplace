<% if params[:page_no].to_i > 1 %>
    $(".nft_collection").append('<%= j(render partial: 'collection_card', locals: {collections: @tokens}) %>');
<% else %>
    $(".nft_collection").html('<%= j(render partial: 'collection_card', locals: {collections: @tokens}) %>');
<% end %>

$(".collection-load-more").data("page-no", "<%= params[:page_no].to_i + 1 %>")

<% if params[:page_no].to_i == @tokens.total_pages %>
  $(".collection-load-more").closest(".row").hide()
<% end %>